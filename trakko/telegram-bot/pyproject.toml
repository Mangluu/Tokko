[project]
name = "telegram-hermes-bridge"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "python-telegram-bot==21.4",
  "httpx==0.27.2",
  "python-dotenv==1.0.1",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
